OPENAI_API_KEY=
GEMINI_API_KEY=
# Model for LLM-backed intent classification and business context extraction.
# Supported prefixes: openai: or gemini:
# Leave empty to use heuristic-only mode (no LLM calls).
AGENT_MODEL=openai:gpt-4o-mini
AGENT_API_TOKEN=change-me
HOST=127.0.0.1
PORT=8081
MCP_URL=http://127.0.0.1:8000/mcp
SESSION_TOKEN=change-me
